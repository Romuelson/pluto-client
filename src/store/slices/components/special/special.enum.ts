export enum SpecialAPI {
	getSpecialInfo = '/api/special/info',
}

export const enum SpecialPrefixType {
	getSpecialInfo = 'special/getSpecialInfo',
}

export const enum SpecialRecipient {
	home = 'home',
}

export enum SpecialServiceType {
	recipient = 'recipient',
}
